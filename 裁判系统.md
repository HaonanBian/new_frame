# 热量限制与功率控制功能移植说明

本文档详细说明了从 `bubin4.9.2.lianmengsai` 移植到 `new_frame_clean` 框架的热量限制和功率控制功能。

---

## 一、功能概述

### 1.1 热量限制 (Heat Limit)
- **功能**: 防止枪口热量超过裁判系统限制，避免被扣血
- **原理**: 实时检测当前枪口热量，当剩余热量不足以发射下一发子弹时，停止发射机构

### 1.2 功率控制 (Power Control)
- **功能**: 限制底盘电机总功率不超过裁判系统允许值
- **原理**: 使用缓冲能量PID动态调整可用功率，通过电机功率模型计算最优电流分配

---

## 二、修改的文件列表

| 文件路径 | 修改类型 | 说明 |
|---------|---------|------|
| [`application/robot_def.h`](application/robot_def.h) | 新增枚举和结构体 | 添加热量限制状态枚举，反馈数据结构体 |
| [`application/shoot/shoot.h`](application/shoot/shoot.h) | 新增宏定义和函数声明 | 热量限制相关宏，获取热量状态函数 |
| [`application/shoot/shoot.c`](application/shoot/shoot.c) | 新增热量限制逻辑 | 热量检测函数，拨盘/摩擦轮控制 |
| [`modules/motor/power_control.h`](modules/motor/power_control.h) | 新增函数声明 | 裁判系统数据接口，辅助函数 |
| [`modules/motor/power_control.c`](modules/motor/power_control.c) | 新增功率控制算法 | 缓冲能量PID，功率限制核心算法 |
| [`application/chassis/chassis.c`](application/chassis/chassis.c) | 集成裁判系统 | 初始化裁判系统，更新功率数据 |
| [`modules/referee/referee_task.c`](modules/referee/referee_task.c) | 新增函数 | 获取裁判系统数据指针 |
| [`modules/referee/referee_task.h`](modules/referee/referee_task.h) | 新增函数声明 | GetRefereeData() |

---

## 三、热量限制详解

### 3.1 相关代码文件

| 代码位置 | 文件 | 行号 | 说明 |
|---------|------|------|------|
| [热量限制状态枚举](application/robot_def.h#L115-120) | robot_def.h | 115-120 | 枚举类型定义 |
| [Shoot_Upload_Data_s结构体](application/robot_def.h#L222-228) | robot_def.h | 222-228 | 反馈数据结构体 |
| [热量限制宏定义](application/shoot/shoot.h#L9-23) | shoot.h | 9-23 | 热量相关宏定义 |
| [热量检测函数](application/shoot/shoot.c#L30-47) | shoot.c | 30-47 | ShootHeatLimitCheck() |
| [ShootTask热量逻辑](application/shoot/shoot.c#L179-226) | shoot.c | 179-226 | 热量限制控制逻辑 |

### 3.2 robot_def.h - 类型定义

跳转到: [`robot_def.h#L115-120`](application/robot_def.h#L115-120)

```c
// 热量限制状态
typedef enum
{
    HEAT_OK = 0,           // 热量正常，可以发射
    HEAT_LIMITED,          // 热量受限，禁止发射
} heat_limit_status_e;
```

**逻辑说明**: 
- `HEAT_OK`:  当前热量允许发射，机器人可以正常射击
- `HEAT_LIMITED`:  热量已接近上限，下一发子弹会导致超热量

跳转到: [`robot_def.h#L222-228`](application/robot_def.h#L222-228)

```c
typedef struct
{
    // ...
    heat_limit_status_e heat_status;  // 热量限制状态
    uint16_t rest_heat;              // 剩余可用热量
} Shoot_Upload_Data_s;
```

### 3.3 shoot.h - 宏定义

跳转到: [`shoot.h#L9-23`](application/shoot/shoot.h#L9-23)

```c
#define BULLET_HEAT_ESTIMATE 41          // 单发子弹预估热量
#define HEAT_SAFE_MARGIN 20              // 热量安全余量

#define FRIC_SPEED_PID_KP        20.0f
#define FRIC_SPEED_PID_KI        1.0f
#define FRIC_SPEED_PID_KD        0.0f
#define FRIC_PID_MAX_OUT         15000.0f
#define FRIC_PID_MAX_IOUT        10000.0f

#define BUFFER_PID_KP            0.1f
#define BUFFER_PID_KI            0.0f
#define BUFFER_PID_KD            0.0f
#define BUFFER_PID_MAX_OUT       1000.0f

#define BUFFER_TARGET_VALUE      30.0f    // 缓冲能量目标值
```

### 3.4 shoot.c - 热量限制实现

跳转到: [`shoot.c#L30-47`](application/shoot/shoot.c#L30-47)

```c
// 热量限制检测:当前热量 + 单发预估热量 > 热量上限 则禁止发射
static heat_limit_status_e ShootHeatLimitCheck(void)
{
    // 空指针检查
    if (referee_data == NULL) {
        return HEAT_OK; // 默认允许发射
    }
    
    // 从裁判系统获取当前热量和热量上限
    uint16_t current_heat = referee_data->PowerHeatData.shooter_17mm_1_barrel_heat;
    uint16_t heat_limit = referee_data->GameRobotState.shooter_barrel_heat_limit;
    
    // 当前热量 + 预估单发热量 > 上限，禁止发射
    if (current_heat + BULLET_HEAT_ESTIMATE > heat_limit) {
        return HEAT_LIMITED;
    }
    return HEAT_OK;
}
```

跳转到: [`shoot.c#L179-226`](application/shoot/shoot.c#L179-226)

```c
// 若不在休眠状态,根据robotCMD传来的控制模式进行拨盘电机参考值设定和模式切换
// 热量限制:当热量受限时不执行拨盘动作
if (heat_status != HEAT_LIMITED)
{
    switch (shoot_cmd_recv.load_mode)
    {
    // 停止拨盘
    case LOAD_STOP:
        DJIMotorOuterLoop(loader, SPEED_LOOP);
        DJIMotorSetRef(loader, 0);
        break;
    // 单发模式
    case LOAD_1_BULLET:
        DJIMotorOuterLoop(loader, ANGLE_LOOP);
        DJIMotorSetRef(loader, loader->measure.total_angle + ONE_BULLET_DELTA_ANGLE);
        hibernate_time = DWT_GetTimeline_ms();
        dead_time = 150;
        break;
    // ... 其他模式
    }
}
else
{
    // 热量受限时停止拨盘
    DJIMotorOuterLoop(loader, SPEED_LOOP);
    DJIMotorSetRef(loader, 0);
}
```

跳转到: [`shoot.c#L228-257`](application/shoot/shoot.c#L228-257)

```c
// 确定是否开启摩擦轮,后续可能修改为键鼠模式下始终开启摩擦轮
// 热量限制:当热量受限时关闭摩擦轮
if (shoot_cmd_recv.friction_mode == FRICTION_ON && heat_status != HEAT_LIMITED)
{
    // 根据收到的弹速设置设定摩擦轮电机参考值
    switch (shoot_cmd_recv.bullet_speed)
    {
    case SMALL_AMU_15:
        DJIMotorSetRef(friction_l, 35000);
        DJIMotorSetRef(friction_r, 35000);
        break;
    case SMALL_AMU_18:
        DJIMotorSetRef(friction_l, 40000);
        DJIMotorSetRef(friction_r, 40000);
        break;
    case SMALL_AMU_30:
        DJIMotorSetRef(friction_l, 60000);
        DJIMotorSetRef(friction_r, 60000);
        break;
    default:
        DJIMotorSetRef(friction_l, 35000);
        DJIMotorSetRef(friction_r, 35000);
        break;
    }
}
else // 关闭摩擦轮或热量受限
{
    DJIMotorSetRef(friction_l, 0);
    DJIMotorSetRef(friction_r, 0);
}
```

---

## 四、功率控制详解

### 4.1 相关代码文件

| 代码位置 | 文件 | 行号 | 说明 |
|---------|------|------|------|
| [PowerControl_RefereeData_s结构体](modules/motor/power_control.h#L28-32) | power_control.h | 28-32 | 裁判系统数据结构 |
| [SetBufferPID函数声明](modules/motor/power_control.h#L51-58) | power_control.h | 51-58 | 设置缓冲能量PID |
| [UpdatePowerControlRefereeData声明](modules/motor/power_control.h#L60-67) | power_control.h | 60-67 | 更新裁判系统数据 |
| [GetChassisPowerLimit声明](modules/motor/power_control.h#L69-74) | power_control.h | 69-74 | 获取功率限制 |
| [缓冲能量PID变量](modules/motor/power_control.c#L26-27) | power_control.c | 26-27 | 静态变量 |
| [裁判系统数据变量](modules/motor/power_control.c#L28-29) | power_control.c | 28-29 | referee_power_data |
| [PowerControlInit初始化](modules/motor/power_control.c#L231-242) | power_control.c | 231-242 | 缓冲能量PID初始化 |
| [PowerControl功率限制](modules/motor/power_control.c#L247-378) | power_control.c | 247-378 | 功率限制核心算法 |
| [SetBufferPID实现](modules/motor/power_control.c#L404-421) | power_control.c | 404-421 | 设置PID参数 |
| [UpdatePowerControlRefereeData实现](modules/motor/power_control.c#L423-435) | power_control.c | 423-435 | 更新裁判数据 |
| [GetChassisPowerLimit实现](modules/motor/power_control.c#L437-445) | power_control.c | 437-445 | 获取功率限制 |
| [GetChassisPower实现](modules/motor/power_control.c#L447-455) | power_control.c | 447-455 | 获取当前功率 |
| [GetChassisPowerBuffer实现](modules/motor/power_control.c#L457-465) | power_control.c | 457-465 | 获取缓冲能量 |

### 4.2 power_control.h - 接口定义

跳转到: [`power_control.h#L28-32`](modules/motor/power_control.h#L28-32)

```c
typedef struct {
    float chassis_power;           // 底盘实时功率(W)
    float chassis_power_buffer;   // 缓冲能量(J)
    uint16_t chassis_power_limit; // 功率上限(W)
} PowerControl_RefereeData_s;
```

跳转到: [`power_control.h#L51-88`](modules/motor/power_control.h#L51-88)

```c
void SetBufferPID(float kp, float ki, float kd);
void UpdatePowerControlRefereeData(float power, float buffer, uint16_t limit);
uint16_t GetChassisPowerLimit(void);
float GetChassisPower(void);
float GetChassisPowerBuffer(void);
```

### 4.3 power_control.c - 功率控制核心算法

跳转到: [`power_control.c#L26-32`](modules/motor/power_control.c#L26-32)

```c
// 缓冲能量PID
static PIDInstance buffer_PID;
// 裁判系统数据
static PowerControl_RefereeData_s referee_power_data = {0};

// 缓冲能量PID参数
static float buffer_kp = 0.1f, buffer_ki = 0.0f, buffer_kd = 0.0f;
```

跳转到: [`power_control.c#L231-242`](modules/motor/power_control.c#L231-242)

```c
// 初始化缓冲能量PID(仅在第一个电机初始化时执行)
if (idx == 1) {
    PID_Init_Config_s buffer_pid_config = {
        .Kp = buffer_kp,
        .Ki = buffer_ki,
        .Kd = buffer_kd,
        .IntegralLimit = 1000,
        .Improve = PID_Trapezoid_Intergral | PID_Integral_Limit | PID_Derivative_On_Measurement,
        .MaxOut = 1000,
    };
    PIDInit(&buffer_PID, &buffer_pid_config);
}
```

跳转到: [`power_control.c#L259-272`](modules/motor/power_control.c#L259-272)

```c
// 缓冲能量PID计算
float buffer_pid_out = 0.0f;
float available_power = referee_power_data.chassis_power_limit;

if (referee_power_data.chassis_power_limit > 0) {
    PIDCalculate(&buffer_PID, referee_power_data.chassis_power_buffer, BUFFER_TARGET);
    buffer_pid_out = buffer_PID.Output;
    
    // 计算实际可用功率限制
    available_power = referee_power_data.chassis_power_limit - buffer_pid_out;
    if (available_power < referee_power_data.chassis_power_limit * POWER_SCALE_MIN) {
        available_power = referee_power_data.chassis_power_limit * POWER_SCALE_MIN;
    }
}
```

跳转到: [`power_control.c#L326-378`](modules/motor/power_control.c#L326-378)

```c
// 功率限制核心算法
if (initial_total_power > available_power)
{
    float ratio = available_power / initial_total_power;
    for (uint8_t i = 0; i < idx; i++)
    {
        motor = dji_motor_instance[i];
        measure = &motor->measure;
        pid_measure = measure->speed_aps / 6.0f;
        initial_give_power[i] *= ratio;
        if (initial_give_power[i] < 0)
        {
            continue;
        }
        float a = K1[i];
        float b = TORQUE_COEF * POWER_COEF * pid_measure;
        float c = K2[i] * pid_measure * pid_measure - initial_give_power[i] + constant[i];
        
        // 根据原始输出方向选择计算公式
        if (power_control_out[i] > 0)
        {
            float discriminant = b * b - 4 * a * c;
            if (discriminant >= 0)
            {
                power_control_out[i] = (-b + sqrt(discriminant)) / (2 * a);
                if (power_control_out[i] > 15000)
                {
                    power_control_out[i] = 15000;
                }
            }
            else
            {
                power_control_out[i] = 0;
            }
        }
        else
        {
            float discriminant = b * b - 4 * a * c;
            if (discriminant >= 0)
            {
                power_control_out[i] = (-b - sqrt(discriminant)) / (2 * a);
                if (power_control_out[i] < -15000)
                {
                    power_control_out[i] = -15000;
                }
            }
            else
            {
                power_control_out[i] = 0;
            }
        }
    }
}
```

跳转到: [`power_control.c#L404-421`](modules/motor/power_control.c#L404-421)

```c
void SetBufferPID(float kp, float ki, float kd)
{
    buffer_kp = kp;
    buffer_ki = ki;
    buffer_kd = kd;
    
    // 更新PID参数
    buffer_PID.Kp = kp;
    buffer_PID.Ki = ki;
    buffer_PID.Kd = kd;
}
```

跳转到: [`power_control.c#L423-435`](modules/motor/power_control.c#L423-435)

```c
void UpdatePowerControlRefereeData(float power, float buffer, uint16_t limit)
{
    referee_power_data.chassis_power = power;
    referee_power_data.chassis_power_buffer = buffer;
    referee_power_data.chassis_power_limit = limit;
}
```

---

## 五、底盘集成详解

### 5.1 相关代码文件

| 代码位置 | 文件 | 行号 | 说明 |
|---------|------|------|------|
| [ChassisInit裁判系统初始化](application/chassis/chassis.c#L110) | chassis.c | 110 | 裁判系统初始化 |
| [ChassisTask功率数据更新](application/chassis/chassis.c#L229-240) | chassis.c | 229-240 | 更新功率数据 |
| [UI功率显示数据](application/chassis/chassis.c#L242-243) | chassis.c | 242-243 | 更新UI显示 |
| [热量数据反馈](application/chassis/chassis.c#L245-247) | chassis.c | 245-247 | 热量数据传递给cmd |
| [裁判系统数据获取](application/chassis/chassis.c#L323-327) | chassis.c | 323-327 | 获取裁判系统数据 |

### 5.2 chassis.c - 裁判系统集成

跳转到: [`chassis.c#L110`](application/chassis/chassis.c#L110)

```c
referee_data = UITaskInit(&huart6, &ui_data); // 裁判系统初始化
```

跳转到: [`chassis.c#L229-240`](application/chassis/chassis.c#L229-240)

```c
// 从裁判系统获取功率数据并更新功率控制模块
if (referee_data != NULL && referee_data->GameRobotState.robot_id != 0) {
    UpdatePowerControlRefereeData(
        referee_data->PowerHeatData.chassis_power,
        referee_data->PowerHeatData.buffer_energy,
        referee_data->GameRobotState.chassis_power_limit
    );
    SetPowerLimit(referee_data->GameRobotState.chassis_power_limit);
} else {
    // 裁判系统未连接时使用默认值
    SetPowerLimit(200);
}
```

跳转到: [`chassis.c#L245-247`](application/chassis/chassis.c#L245-247)

```c
// 热量数据获取,传递到cmd用于限制发射
chassis_feedback_data.rest_heat = 
    referee_data->GameRobotState.shooter_barrel_heat_limit 
    - referee_data->PowerHeatData.shooter_17mm_1_barrel_heat;
```

---

## 六、数据共享机制

### 6.1 相关代码文件

| 代码位置 | 文件 | 行号 | 说明 |
|---------|------|------|------|
| [GetRefereeData实现](modules/referee/referee_task.c#L23-28) | referee_task.c | 23-28 | 获取裁判系统指针 |
| [GetRefereeData声明](modules/referee/referee_task.h#L24-29) | referee_task.h | 24-29 | 函数声明 |

### 6.2 referee_task.c/h - 单例模式

跳转到: [`referee_task.c#L23-28`](modules/referee/referee_task.c#L23-28)

```c
/**
 * @brief 获取裁判系统数据指针，供其他模块使用
 */
referee_info_t *GetRefereeData(void)
{
    return referee_recv_info;
}
```

跳转到: [`referee_task.h#L24-29`](modules/referee/referee_task.h#L24-29)

```c
/**
 * @brief 获取裁判系统数据指针，供其他模块使用
 */
referee_info_t *GetRefereeData(void);
```

---

## 七、数据流向图

```
┌─────────────────────────────────────────────────────────────────┐
│                         裁判系统                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │  功率限制(W)   │  │  当前功率(W)   │  │ 缓冲能量(J)   │   │
│  │  跳转: [1]     │  │  跳转: [2]     │  │  跳转: [3]     │   │
│  └───────┬────────┘  └───────┬────────┘  └───────┬────────┘   │
└──────────┼───────────────────┼───────────────────┼──────────────┘
           │                   │                   │
           └───────────────────┴───────────────────┘
                               │
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                        ChassisTask                              │
│  跳转: [4]                                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ UpdatePowerControlRefereeData(power, buffer, limit)    │   │
│  └─────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      PowerControl()                              │
│  跳转: [5]                                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. 缓冲能量PID计算    跳转: [6]                         │   │
│  │ 2. 计算可用功率      跳转: [7]                          │   │
│  │ 3. 电机功率模型计算  跳转: [8]                          │   │
│  │ 4. 超功率时按比例分配 跳转: [9]                         │   │
│  └─────────────────────────────────────────────────────────┘   │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                         底盘电机                                 │
│  4个M3508电机                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         裁判系统                                  │
│  ┌────────────────┐  ┌────────────────┐                        │
│  │ 枪口热量上限   │  │ 当前枪口热量   │                        │
│  │  跳转: [10]     │  │  跳转: [11]     │                        │
│  └───────┬────────┘  └───────┬────────┘                        │
└──────────┼───────────────────┼───────────────────────────────────┘
           │                   │
           └───────────────────┘
                               │
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                        ShootTask                                │
│  跳转: [12]                                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ShootHeatLimitCheck()    跳转: [13]                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                               │                                 │
│          ┌──────────────────┬┴──────────────────┐            │
│          ↓                  ↓                  ↓               │
│  ┌───────────────┐  ┌─────────────┐  ┌──────────────┐         │
│  │ 热量受限?      │  │  热量正常?  │  │ 热量正常?    │         │
│  │  - 关闭摩擦轮 │  │ - 关闭拨盘  │  │ - 正常拨盘   │         │
│  │  - 关闭拨盘  │  │ - 正常摩擦轮│  │ - 正常摩擦轮 │         │
│  └───────────────┘  └─────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

### 图中跳转索引

| 编号 | 跳转位置 | 说明 |
|------|---------|------|
| [1] | [`power_control.c#L28`](modules/motor/power_control.c#L28) | referee_power_data.chassis_power_limit |
| [2] | [`power_control.c#L28`](modules/motor/power_control.c#L28) | referee_power_data.chassis_power |
| [3] | [`power_control.c#L28`](modules/motor/power_control.c#L28) | referee_power_data.chassis_power_buffer |
| [4] | [`chassis.c#L229-240`](application/chassis/chassis.c#L229-240) | ChassisTask功率数据更新 |
| [5] | [`power_control.c#L247-378`](modules/motor/power_control.c#L247-378) | PowerControl核心函数 |
| [6] | [`power_control.c#L259-272`](modules/motor/power_control.c#L259-272) | 缓冲能量PID计算 |
| [7] | [`power_control.c#L268`](modules/motor/power_control.c#L268) | 计算可用功率 |
| [8] | [`power_control.c#L326-378`](modules/motor/power_control.c#L326-378) | 电机功率模型计算 |
| [9] | [`power_control.c#L329`](modules/motor/power_control.c#L329) | 按比例分配功率 |
| [10] | [`shoot.c#L40`](application/shoot/shoot.c#L40) | referee_data->GameRobotState.shooter_barrel_heat_limit |
| [11] | [`shoot.c#L39`](application/shoot/shoot.c#L39) | referee_data->PowerHeatData.shooter_17mm_1_barrel_heat |
| [12] | [`shoot.c#L179-226`](application/shoot/shoot.c#L179-226) | ShootTask热量控制 |
| [13] | [`shoot.c#L30-47`](application/shoot/shoot.c#L30-47) | ShootHeatLimitCheck函数 |

---

## 八、参数调优建议

### 8.1 热量相关参数

| 参数 | 默认值 | 调整范围 | 代码位置 |
|------|-------|---------|---------|
| `BULLET_HEAT_ESTIMATE` | 41 | 30-50 | [`shoot.h#L9`](application/shoot/shoot.h#L9) |
| `HEAT_SAFE_MARGIN` | 20 | 10-30 | [`shoot.h#L10`](application/shoot/shoot.h#L10) |

**调优方法**: 
1. 实际射击测试，记录每次发射后的热量增量
2. 观察是否会触发热量限制
3. 调整 `BULLET_HEAT_ESTIMATE` 使检测更准确

### 8.2 功率相关参数

| 参数 | 默认值 | 调整范围 | 代码位置 |
|------|-------|---------|---------|
| `BUFFER_TARGET` | 30.0f | 20-50 | [`power_control.c#L15`](modules/motor/power_control.c#L15) |
| `buffer_kp` | 0.1f | 0.05-0.3 | [`power_control.c#L32`](modules/motor/power_control.c#L32) |
| `POWER_SCALE_MIN` | 0.1f | 0.05-0.2 | [`power_control.c#L16`](modules/motor/power_control.c#L16) |

**调优方法**: 
1. 观察底盘运动时的缓冲能量变化
2. 如果缓冲能量下降太快 → 减小 `BUFFER_TARGET` 或减小 `buffer_kp`
3. 如果缓冲能量一直很高 → 增大 `BUFFER_TARGET`

---

## 九、调试技巧

### 9.1 热量限制调试
跳转到: [`shoot.c#L30-47`](application/shoot/shoot.c#L30-47)

```c
// 在ShootHeatLimitCheck中添加调试信息
static heat_limit_status_e ShootHeatLimitCheck(void)
{
    if (referee_data == NULL) {
        return HEAT_OK;
    }
    
    uint16_t current_heat = referee_data->PowerHeatData.shooter_17mm_1_barrel_heat;
    uint16_t heat_limit = referee_data->GameRobotState.shooter_barrel_heat_limit;
    
    // 添加调试打印
    // printf("Heat: %d/%d, Available: %d\r\n", 
    //        current_heat, heat_limit, heat_limit - current_heat);
    
    if (current_heat + BULLET_HEAT_ESTIMATE > heat_limit) {
        return HEAT_LIMITED;
    }
    return HEAT_OK;
}
```

### 9.2 功率控制调试
跳转到: [`power_control.c#L259-272`](modules/motor/power_control.c#L259-272)

```c
// 在PowerControl中添加调试信息
if (referee_power_data.chassis_power_limit > 0) {
    PIDCalculate(&buffer_PID, referee_power_data.chassis_power_buffer, BUFFER_TARGET);
    buffer_pid_out = buffer_PID.Output;
    
    // 添加调试打印
    // printf("BufferPID: %.1f, Available: %.1f\r\n", 
    //        buffer_pid_out, available_power);
    
    available_power = referee_power_data.chassis_power_limit - buffer_pid_out;
    if (available_power < referee_power_data.chassis_power_limit * POWER_SCALE_MIN) {
        available_power = referee_power_data.chassis_power_limit * POWER_SCALE_MIN;
    }
}
```

### 9.3 常见问题

| 问题现象 | 可能原因 | 解决方案 |
|---------|---------|---------|
| 摩擦轮突然停止 | 热量限制触发 | 检查当前热量和限制 [`shoot.c#L39-40`](application/shoot/shoot.c#L39-40) |
| 底盘加速迟缓 | 功率限制过严 | 增大 `BUFFER_TARGET` [`power_control.c#L15`](modules/motor/power_control.c#L15) |
| 缓冲能量一直为0 | 裁判系统未连接 | 检查串口和协议解析 [`referee_task.c#L23-28`](modules/referee/referee_task.c#L23-28) |
| 功率忽高忽低 | PID参数不当 | 调整 `buffer_kp` [`power_control.c#L32`](modules/motor/power_control.c#L32) |

---

## 十、核心算法公式

### 10.1 热量限制公式

$$允许发射 = (当前热量 + 单发热量) \leq 热量上限$$

### 10.2 功率模型公式

DJI电机输出功率可以表示为:

$$P = K_1 \cdot I^2 + K_2 \cdot \omega^2 + K_3 \cdot I \cdot \omega + C$$

其中:
- $I$: 电机电流
- $\omega$: 电机转速(rpm)
- $K_1, K_2, K_3, C$: 电机固有常数

### 10.3 缓冲能量PID控制

```
           ┌──────────────┐
           │   目标值      │
           │ BUFFER_TARGET│ (30J)
           └──────┬───────┘
                  │
                  ↓ (设定值)
         ┌────────────────┐
         │   PID计算     │ → buffer_pid_out
         └────────┬───────┘
                  │
                  ↓
        ┌─────────────────┐
        │ 实际可用功率    │
        │ = 功率上限      │
        │  - buffer_pid  │
        └─────────────────┘
```

---

## 十一、总结

### 11.1 设计原则

1. **单例模式**:  裁判系统只初始化一次，通过 `GetRefereeData()` 共享
   - 初始化: [`chassis.c#L110`](application/chassis/chassis.c#L110)
   - 获取: [`shoot.c#L57`](application/shoot/shoot.c#L57)

2. **空指针保护**:  所有裁判系统数据访问前检查指针有效性
   - [`shoot.c#L34`](application/shoot/shoot.c#L34)
   - [`shoot.c#L153`](application/shoot/shoot.c#L153)
   - [`chassis.c#L230`](application/chassis/chassis.c#L230)

3. **安全边界**:  设置最小功率缩放系数，防止完全失速
   - [`power_control.c#L269-271`](modules/motor/power_control.c#L269-271)

### 11.2 性能影响

| 功能 | 频率 | CPU占用 | 代码位置 |
|------|------|--------|---------|
| 热量检测 | 200Hz | <0.5% | [`shoot.c#L30-47`](application/shoot/shoot.c#L30-47) |
| 功率控制 | 200Hz | <1% | [`power_control.c#L247-378`](modules/motor/power_control.c#L247-378) |

---

*文档更新时间: 2026-03-21*
