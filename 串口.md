# RoboMaster 2026 机甲大师高校系列赛通信协议 V1.3.0
**文档版本**：V1.3.0（20260327）
**编制**：RoboMaster组委会
**版权**：© 2026 大疆

---

## 修改日志
| 日期 | 版本 | 修订记录 |
| --- | --- | --- |
| 2026.03.26 | V1.3.0 | 修订命令字:0x0101,0x0305；修订自定义客户端协议中指令:CustomControl,RadarInfoToClient,AirSupportCommand,MapClickInfoNotify,RuneStatusSync,Event,RobotPosition,SentryCtrlCommand,SentryCtrlResult,TechCoreMotionStateSync；修改了自定义客户端IP配置方式 |
| 2026.02.09 | V1.2.0 | 修订命令0x0105,0x0204,0x0209,0x020C,0x020D,0x0309,0x0310；删除命令码0x0304；修订自定义客户端协议中原RemoteControl(现拆分为KeyboardMouseControl与CustomControl)；自定义客户端新增CommonCommand指令；修复了多处描述或命名错误 |
| 2025.12.18 | V1.1.0 | 补充雷达无线链路相关说明 |
| 2025.11.27 | V1.0.0 | 首次发布 |

---

## 前言
1. 本通信协议在 RMUC 与 RMUL 两项赛事中的适用范围如下:
   - 对于在 RMUC 与 RMUL 之间共用的兵种、机制或场地道具等条目，本协议内容对两项赛事均适用；
   - 对于仅适用于某一赛项的兵种、机制或场地道具等条目，默认不适用于另一赛项。
2. © 2026 大疆 版权所有

---

# 1 串口协议
## 1.1 串口协议格式
通信方式：8 位数据位，1 位停止位，无硬件流控，无校验位。

### 表 1-1 波特率
| 物理接口 | 波特率 |
| --- | --- |
| 电源管理模块 ↔ 机器人 | 115200 |
| 自定义控制器 ↔ 选手端 | 115200 |
| 图传发送端 ↔ 机器人 | 921600 |

### 表 1-2 通信协议格式
| frame_header | cmd_id | data | frame_tail |
| --- | --- | --- | --- |
| 5-byte | 2-byte | n-byte | 2-byte，CRC16，整包校验 |

### 表 1-3 frame_header 格式
| SOF | data_length | CRC8 |
| --- | --- | --- |
| 1-byte | 2-byte | 1-byte |

### 表 1-4 帧头详细定义
| 域 | 偏移位置 | 大小(字节) | 详细描述 |
| --- | --- | --- | --- |
| SOF | 0 | 1 | 数据帧起始字节，固定值为0xA5 |
| data_length | 1 | 2 | 数据帧中data的长度 |
| seq | 3 | 1 | 包序号 |
| CRC8 | 4 | 1 | 帧头CRC8校验 |

### 链路说明
- 常规链路：裁判系统服务器 ↔ 主控模块 ↔ 电源管理模块User串口
- 图传链路：裁判系统选手端 ↔ 图传模块 ↔ 机器人
- 雷达无线链路：裁判系统信号发射源 → 雷达
- 正常延迟约130ms，丢包率<1%；恶劣环境延迟约200ms，丢包率约3%

---

## 1.2 命令码ID 和常规链路数据说明
### 表 1-5 命令码ID 一览
| 命令码 | 数据段长度 | 说明 | 发送方/接收方 | 所属数据链路 |
| --- | --- | --- | --- | --- |
| 0x0001 | 11 | 比赛状态数据，1Hz | 服务器→全体机器人 | 常规链路 |
| 0x0002 | 1 | 比赛结果数据，结束触发 | 服务器→全体机器人 | 常规链路 |
| 0x0003 | 16 | 机器人血量数据，3Hz | 服务器→全体机器人 | 常规链路 |
| 0x0101 | 4 | 场地事件数据，1Hz | 服务器→己方全体机器人 | 常规链路 |
| 0x0104 | 3 | 裁判警告数据 | 服务器→被判罚方 | 常规链路 |
| 0x0105 | 3 | 飞镖发射相关数据，1Hz | 服务器→己方全体机器人 | 常规链路 |
| 0x0201 | 13 | 机器人性能体系数据，10Hz | 主控模块→对应机器人 | 常规链路 |
| 0x0202 | 14 | 缓冲能量&射击热量，10Hz | 主控模块→对应机器人 | 常规链路 |
| 0x0203 | 16 | 机器人位置数据，1Hz | 主控模块→对应机器人 | 常规链路 |
| 0x0204 | 8 | 增益&底盘能量，3Hz | 服务器→对应机器人 | 常规链路 |
| 0x0206 | 1 | 伤害状态数据，触发发送 | 主控模块→对应机器人 | 常规链路 |
| 0x0207 | 7 | 实时射击数据，发射后发送 | 主控模块→对应机器人 | 常规链路 |
| 0x0208 | 6 | 允许发弹量，10Hz | 服务器→英雄/步兵/哨兵/空中 | 常规链路 |
| 0x0209 | 5 | RFID状态，3Hz | 服务器→己方RFID机器人 | 常规链路 |
| 0x020A | 6 | 飞镖选手端指令，3Hz | 服务器→己方飞镖 | 常规链路 |
| 0x020B | 40 | 地面机器人位置，1Hz | 服务器→己方哨兵 | 常规链路 |
| 0x020C | 2 | 雷达标记进度，1Hz | 服务器→己方雷达 | 常规链路 |
| 0x020D | 6 | 哨兵自主决策同步，1Hz | 服务器→己方哨兵 | 常规链路 |
| 0x020E | 1 | 雷达自主决策同步，1Hz | 服务器→己方雷达 | 常规链路 |
| 0x0301 | 118 | 机器人交互数据，≤30Hz | - | 常规链路 |
| 0x0302 | 30 | 自定义控制器→机器人，≤30Hz | 自定义控制器→机器人 | 图传链路 |
| 0x0303 | 15 | 小地图交互，选手端触发 | 选手端→服务器→机器人 | 常规链路 |
| 0x0305 | 48 | 小地图雷达数据，≤5Hz | 雷达→服务器→选手端 | 常规链路 |
| 0x0306 | 8 | 自定义控制器↔选手端，≤30Hz | 自定义控制器↔选手端 | - |
| 0x0307 | 103 | 小地图路径数据，≤1Hz | 哨兵→选手端 | 常规链路 |
| 0x0308 | 34 | 小地图机器人数据，≤3Hz | 己方机器人→选手端 | 常规链路 |
| 0x0309 | 30 | 机器人→自定义控制器，≤10Hz | 机器人→自定义控制器 | 图传链路 |
| 0x0310 | 300 | 机器人→自定义客户端，≤50Hz | 机器人→自定义客户端 | 图传链路 |
| 0x0311 | 30 | 自定义客户端→机器人，≤75Hz | 自定义客户端→机器人 | 图传链路 |
| 0x0A01 | 24 | 对方位置，10Hz | 信号发射源→雷达 | 雷达无线链路 |
| 0x0A02 | 12 | 对方血量，10Hz | 信号发射源→雷达 | 雷达无线链路 |
| 0x0A03 | 10 | 对方发弹量，10Hz | 信号发射源→雷达 | 雷达无线链路 |
| 0x0A04 | 8 | 对方宏观状态，10Hz | 信号发射源→雷达 | 雷达无线链路 |
| 0x0A05 | 36 | 对方增益，10Hz | 信号发射源→雷达 | 雷达无线链路 |
| 0x0A06 | 6 | 对方干扰波密钥，10Hz | 信号发射源→雷达 | 雷达无线链路 |

### 关键命令结构体（精简）
```c
// 0x0001 比赛状态
typedef _packed struct {
    uint8_t game_type : 4;
    uint8_t game_progress : 4;
    uint16_t stage_remain_time;
    uint64_t SyncTimeStamp;
} game_status_t;

// 0x0003 机器人血量
typedef _packed struct {
    uint16_t ally_1_robot_HP;
    uint16_t ally_2_robot_HP;
    uint16_t ally_3_robot_HP;
    uint16_t ally_4_robot_HP;
    uint16_t reserved;
    uint16_t ally_7_robot_HP;
    uint16_t ally_outpost_HP;
    uint16_t ally_base_HP;
} game_robot_HP_t;
```

---

## 1.3 小地图交互数据
### 1.3.1 选手端下发数据（0x0303）
- 发送间隔：普通≥0.5s，半自动≥3s
- 发送机制：触发后连续发5次（100ms间隔），之后1Hz定频
- 结构体：
```c
typedef _packed struct {
    float target_position_x;
    float target_position_y;
    uint8_t cmd_keyboard;
    uint8_t target_robot_id;
    uint16_t cmd_source;
} map_command_t;
```

### 1.3.2 选手端接收数据
- 0x0305：雷达上传敌我机器人位置（cm）
- 0x0307：哨兵路径规划（dm）
- 0x0308：机器人→选手端自定义消息（UTF-16）

---

## 1.4 图传链路数据说明
- 0x0302/0x0309：自定义控制器 ↔ 机器人（30字节）
- 0x0310/0x0311：自定义客户端 ↔ 机器人（300/30字节）

---

## 1.5 非链路数据说明（0x0306）
自定义控制器模拟键鼠操作选手端：
- 键盘双按键无冲
- 鼠标绝对坐标（1920×1080）
- 左键/右键状态

---

## 1.6 雷达无线链路数据说明
0x0A01~0x0A06 高频推送敌方位置、血量、发弹量、状态、增益、密钥。

---

# 2 自定义客户端协议
- 格式：Protobuf v3
- 传输：MQTT（IP：192.168.12.1，端口：3333）
- 客户端IP：192.168.12.2
- 图传码流：UDP 3334，HEVC

## 2.1 指令概览（核心）
| 指令名 | 用途 | 方向 | 频率 |
| --- | --- | --- | --- |
| KeyboardMouseControl | 键鼠输入 | 客户端→机器人 | 75Hz |
| CustomControl | 自定义数据 | 客户端→机器人 | 75Hz |
| GameStatus | 全局状态 | 服务器→客户端 | 5Hz |
| GlobalUnitStatus | 单位状态 | 服务器→客户端 | 1Hz |
| Event | 事件通知 | 服务器→客户端 | 触发 |
| RobotPosition | 坐标朝向 | 服务器→客户端 | 1Hz |
| CustomByteBlock | 自定义数据流 | 机器人→客户端 | 50Hz |

## 2.2 关键指令定义（精简）
### KeyboardMouseControl
```protobuf
message KeyboardMouseControl {
    optional int32 mouse_x = 1;
    optional int32 mouse_y = 2;
    optional int32 mouse_z = 3;
    optional bool left_button_down = 4;
    optional bool right_button_down = 5;
    optional uint32 keyboard_value = 6;
    optional bool mid_button_down = 7;
}
```

### CustomControl
```protobuf
message CustomControl {
    optional bytes data = 1; // ≤30字节
}
```

### GameStatus
```protobuf
message GameStatus {
    optional uint32 current_round = 1;
    optional uint32 total_rounds = 2;
    optional uint32 red_score = 3;
    optional uint32 blue_score = 4;
    optional uint32 current_stage = 5;
    optional int32 stage_countdown_sec = 6;
    optional bool is_paused = 8;
}
```

---

# 附录一：CRC 校验代码示例
## CRC8
```c
const unsigned char CRC8_INIT = 0xff;
const unsigned char CRC8_TAB[256] = { /* 查表 */ };
unsigned char Get_CRC8_Check_Sum(unsigned char *pchMessage,unsigned int dwLength,unsigned char ucCRC8);
unsigned int Verify_CRC8_Check_Sum(unsigned char *pchMessage, unsigned int dwLength);
void Append_CRC8_Check_Sum(unsigned char *pchMessage, unsigned int dwLength);
```

## CRC16
```c
uint16_t CRC_INIT = 0xffff;
const uint16_t wCRC_Table[256] = { /* 查表 */ };
uint16_t Get_CRC16_Check_Sum(uint8_t *pchMessage,uint32_t dwLength,uint16_t wCRC);
uint32_t Verify_CRC16_Check_Sum(uint8_t *pchMessage, uint32_t dwLength);
void Append_CRC16_Check_Sum(uint8_t * pchMessage,uint32_t dwLength);
```

---

# 附录二：ID 编号说明
## 机器人ID
- 红方：1(英雄)、2(工程)、3/4/5(步兵)、6(空中)、7(哨兵)、8(飞镖)、9(雷达)、10(前哨)、11(基地)
- 蓝方：101~111（对应红方）

## 选手端ID
- 红方：0x0101~0x0106
- 蓝方：0x0165~0x0169
- 服务器：0x8080

---

# 附录三：自定义客户端示例通信代码（C#）
```csharp
using MQTTnet;
using MQTTnet.Client;
public class MyMqttClient {
    private IMqttClient m_MqttClient;
    public MyMqttClient(string clientID, string ip, int port) { /* 连接 */ }
    public void PublishBytesMsg(string topic, byte[] message) { /* 发布 */ }
    public void Subscribe(string topic) { /* 订阅 */ }
}
```

---

# 联系方式
- 邮箱：robomaster@dji.com
- 论坛：https://bbs.robomaster.com
- 官网：https://www.robomaster.com
